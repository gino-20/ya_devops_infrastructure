apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sausage-store.fullname" . }}-test-connection"
  labels:
    {{- include "sausage-store.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sausage-store.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
