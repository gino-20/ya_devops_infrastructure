apiVersion: v1
kind: Service
metadata:
  name: {{ include "sausage-store.fullname" . }}
  labels:
    {{- include "sausage-store.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sausage-store.selectorLabels" . | nindent 4 }}
